# Отчёт по Спринту 6: Импорт, экспорт и безопасный обмен данными

**Период:** 25 мая – 1 июня 2026 года (согласно коммитам).

**Цель:** Реализован безопасный импорт/экспорт хранилища в различных форматах, добавлена поддержка Bitwarden и LastPass, создана система безопасного обмена записями (шаринг) с использованием QR-кодов и публичных ключей.

Спринт 6. На шестом этапе реализуется безопасный обмен данными с:

* экспортом записей в зашифрованном JSON, CSV и Bitwarden JSON;
* импортом из Bitwarden JSON, LastPass CSV и нативного формата;
* автоматическим определением формата файла при импорте;
* шифрованием экспорта через AES-256-GCM с отдельным паролем;
* безопасным шарингом отдельных записей через пароль или публичный ключ (RSA);
* генерацией и сканированием QR-кодов для обмена ключами и пакетами шаринга;
* защитой от formula injection в CSV и санитизацией всех полей;
* защитой от side-channel атак (сравнение за константное время, безопасное затирание памяти);
* экспортом журнала аудита (JSON, CSV, PDF);

## Хронология создания проекта

### 25 мая 2026 — добавлены основные сервисы импорта/экспорта

* `exporter.py` — экспорт хранилища в различные форматы
* `importer.py` — импорт записей с автоопределением формата
* `sharing_service.py` — безопасный обмен записями
* `key_exchange.py` — QR-коды для обмена ключами

### 25 мая 2026 — добавлены GUI-диалоги

* `export_dialog.py` — диалог экспорта с выбором формата и записей
* `import_dialog.py` — диалог импорта с превью и настройками
* `sharing_dialog.py` — диалог шаринга с историей
* `qr_viewer_dialog.py` — просмотр и сканирование QR-кодов

### 25 мая 2026 — интеграция сервисов

* интеграция сервисов в `app.py`
* добавление первоначального набора тестов

### 28 мая 2026 — исправления и улучшения

* улучшено управление паролем при экспорте
* добавлено кэширование расшифрованных записей
* улучшена обработка формул в CSV (защита от injection)
* оптимизирован метод `_export_encrypted_json`
* добавлена проверка конфигурации Argon2 для защиты от DoS-атак

### 31 мая 2026 — рефакторинг и усиление безопасности

* реализована защита от side-channel атак (безопасное затирание памяти, сравнение за константное время)
* добавлен экспорт журнала аудита в JSON, CSV и PDF
* улучшено управление таблицей записей
* добавлены классы `ImportResult`, `ExportOptions`, `ImportOptions`, `SharePermissions`
* выполнен рефакторинг `VaultExporter`, `VaultImporter`, `SharingService`
* добавлены форматы `bitwarden_encrypted`, `NativeJSONFormat`, `CSVVaultFormat`

## Работа файлов

### Сервисы импорта/экспорта (`src/core/import_export/`)

#### `exporter.py` — `VaultExporter`

* `export()` — основной метод экспорта с проверкой пароля
* `_export_encrypted_json()` — экспорт в зашифрованный JSON (AES-256-GCM)
* `_export_csv()` — экспорт в CSV с защитой от formula injection
* `_export_bitwarden()` — экспорт в Bitwarden JSON
* `_get_entries_for_export()` — получение записей с фильтрацией полей
* `_record_history()` — логирование операций

**Новое:**

* экспорт журнала аудита в JSON, CSV и PDF

#### `importer.py` — `VaultImporter`

* `import_file()` — основной метод импорта с лимитами и таймаутом
* `_detect_format()` — автоопределение формата
* `_parse_encrypted_json()` — импорт нативного формата
* `_parse_bitwarden()` — импорт Bitwarden JSON
* `_parse_lastpass_csv()` — импорт LastPass CSV
* `_parse_csv()` — универсальный CSV-импорт
* `_sanitize_entry()` — санитизация данных

**Новое:**

* класс `ImportResult` для унификации результатов импорта

#### `sharing_service.py` — `SharingService`

* `share_entry()` — создание пакета шаринга
* `receive_entry()` — получение и расшифровка пакета
* `_create_password_package()` — PBKDF2 + AES-GCM
* `_create_public_key_package()` — RSA-OAEP + AES-GCM
* `generate_key_pair()` — генерация RSA-2048 ключей
* `save_contact()` / `get_contacts()` — управление контактами
* `revoke_share()` — отзыв шаринга

**Новое:**

* улучшена обработка прав доступа
* упрощена криптографическая логика

#### `key_exchange.py` — `QRCodeService`

* `generate_qr_codes()` — генерация QR-кодов с chunking
* `generate_public_key_qr()` — QR публичного ключа
* `generate_share_qr()` — QR пакета шаринга
* `scan_qr_from_file()` — сканирование QR
* `decode_chunks()` — сборка данных

### Новые классы и модули

#### Классы параметров

* `ImportResult`
* `ExportOptions`
* `ImportOptions`
* `SharePermissions`

#### Исключения

* `ImportExportError`
* `UnsupportedFormatError`
* `ImportValidationError`

#### Модуль форматов

* `NativeJSONFormat`
* `CSVVaultFormat`
* `BitwardenFormat`
* `LastPassFormat`

#### Безопасность

* `secure_zero_memory()`
* `constant_time_compare()`

### GUI-диалоги (`src/gui/`)

#### `export_dialog.py`

* выбор формата (`encrypted_json`, `csv`, `bitwarden`, `bitwarden_encrypted`)
* ввод пароля для зашифрованного формата
* мультивыбор записей
* исключение заметок и тегов
* GZIP-сжатие
* предупреждения для открытых форматов

**Новое:** экспорт журнала аудита.

#### `import_dialog.py`

* автоопределение формата
* режимы `merge`, `replace`, `dry_run`
* таблица предпросмотра
* отображение ошибок
* фоновое выполнение операций

#### `sharing_dialog.py`

Вкладки:

1. **Поделиться**
2. **История**
3. **Получить**

Функции:

* выбор контактов
* создание шаринга
* просмотр активных шарингов
* отзыв шаринга

#### `qr_viewer_dialog.py`

Вкладки:

1. **Показать QR**
2. **Сканировать**

Функции:

* отображение SVG QR-кодов
* навигация по чанкам
* TTL-индикатор
* сохранение и копирование QR

### Диалог настроек

Доработан 31 мая:

* профили безопасности:

  * Быстрый
  * Сбалансированный
  * Высокий
* оптимизация таймаутов
* обновлён механизм загрузки и сохранения конфигурации

## Тестирование (`tests/`)

### Базовые тесты

* `test_roundtrip_encrypted_json`
* `test_roundtrip_csv`
* `test_roundtrip_bitwarden`
* `test_import_lastpass_csv`
* `test_sharing_password_roundtrip`
* `test_sharing_public_key_roundtrip`
* `test_sharing_tamper_detection`
* `test_qr_roundtrip`
* `test_performance_export_1000`
* `test_performance_import_1000`

### Добавлено 31 мая – 1 июня

#### PanicMode

* очистка буфера обмена
* блокировка хранилища
* запись в аудит-лог
* скрытие окна
* идемпотентность активации

#### ActivityMonitor

* автоматическая блокировка
* сброс таймера активности
* изменение конфигурации
* потокобезопасность

#### Импорт/экспорт и шаринг

* проверка целостности данных
* проверка форматов
* тесты производительности

## Выполненные требования (из ТЗ Спринта 6)

### Архитектура

**ARC-1, ARC-2, ARC-3**

Создан модуль `src/core/import_export/`, реализовано разделение ключей и независимые сервисы импорта, экспорта и шаринга.

### Экспорт

**EXP-1, EXP-2, EXP-3, EXP-4**

* encrypted_json
* CSV
* Bitwarden JSON
* AES-256-GCM
* выбор записей
* исключение полей
* GZIP-сжатие
* экспорт аудита

### Импорт

**IMP-1, IMP-2, IMP-3, IMP-4**

* encrypted_json
* CSV
* Bitwarden JSON
* LastPass CSV
* лимит 10 МБ
* таймаут 30 секунд
* merge / replace / dry_run

### Шаринг

**SHR-1, SHR-2, SHR-3, SHR-4**

* пароль
* публичный ключ
* права доступа
* срок действия
* полный workflow отправителя и получателя

### QR-коды

**QR-1, QR-2, QR-3, QR-4**

* публичные ключи
* пакеты шаринга
* импорт из файлов
* TTL + nonce

### Криптография

**CRY-1, CRY-2, CRY-3, CRY-4**

* PBKDF2-HMAC-SHA256 (100 000 итераций)
* RSA-OAEP + AES-256-GCM
* forward secrecy
* HMAC и цифровые подписи

Дополнительно:

* защита от side-channel атак

### База данных

**DB-1, DB-2, DB-3**

Добавлены таблицы:

* `shared_entries`
* `import_export_history`
* `contacts`

### Форматы файлов

**FMT-1, FMT-2, FMT-3**

* SHA-256 верификация
* цифровые подписи
* формат `bitwarden_encrypted`

### Тестирование

**TEST-1 – TEST-5**

Реализованы:

* round-trip
* interoperability
* sharing security
* QR code
* performance
* PanicMode
* ActivityMonitor

### Безопасность

**SEC-1, SEC-2, SEC-3, SEC-4, SEC-5**

* шифрованный экспорт по умолчанию
* санитизация данных
* разделение ключей
* очистка ключей из памяти
* защита от вредоносных паттернов


## Ключевые технические решения

1. Автоопределение формата по содержимому и расширению файла.
2. Защита от formula injection через префикс `'` перед символами `=`, `+`, `-`, `@`.
3. Гибридное шифрование: RSA-OAEP + AES-GCM.
4. QR chunking с контрольными суммами.
5. SHA-256 для контроля целостности.
6. Асинхронное выполнение длительных операций.
7. Защита от side-channel атак через `secure_zero_memory()` и `constant_time_compare()`.
8. Профили безопасности для различных сценариев использования.
9. Экспорт аудита в JSON, CSV и PDF.


## Итог по спринту

По собственным ощущениям выполнено на **95%** (

### Выполнено

* ✅ Экспорт в 3 форматах (`encrypted_json`, `csv`, `bitwarden`) + экспорт аудита
* ✅ Импорт из 4 форматов (`encrypted_json`, `csv`, `bitwarden`, `lastpass_csv`)
* ✅ Безопасный шаринг через пароль и публичный ключ
* ✅ QR-коды для обмена ключами и пакетами
* ✅ Полная интеграция с GUI
* ✅ Профили безопасности
* ✅ Защита от side-channel атак
* ✅ Тестирование производительности и безопасности
* ✅ Тестирование PanicMode и ActivityMonitor

### Трудности

* Совместимость с Bitwarden JSON потребовала нескольких итераций исправлений.
* QR-коды с chunking оказались сложнее ожидаемого из-за ограничений библиотек.
* Синхронизация потоков при импорте/экспорте и в ActivityMonitor требовала дополнительной проработки.
* Реализация защиты от side-channel атак без заметной потери производительности потребовала низкоуровневых оптимизаций.

*Конец работы со Спринтом 6.*
